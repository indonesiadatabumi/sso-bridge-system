# Token Lifecycle - Visual Diagram

## 🔄 Complete Token Flow with Redis TTL

```
┌─────────────────────────────────────────────────────────────────────┐
│                        User Login Flow                               │
└─────────────────────────────────────────────────────────────────────┘

User → Login Request → Backend API
                          ↓
                    ┌─────────────────┐
                    │ Generate Tokens │
                    │  - Access Token │
                    │  - Refresh Token│
                    │  - ID Token     │
                    └────────┬────────┘
                             ↓
              ┌──────────────────────────────┐
              │  Store in Redis with TTL     │
              ├──────────────────────────────┤
              │ Access Token:  1 hour TTL   │
              │ Refresh Token: 30 days TTL  │
              │ ID Token:       1 hour TTL  │
              └──────────────────────────────┘
                             ↓
              ┌──────────────────────────────┐
              │  Backup in PostgreSQL        │
              │  (for audit trail)           │
              └──────────────────────────────┘
                             ↓
                    Return Tokens to User
```

---

## ⏰ Token Expiration Timeline

### Access Token (Short-lived)

```
Time: 0:00:00
┌─────────────────────────────────────────────────────┐
│  Redis: SETEX sso:token:abc123 3600 {...}           │
│  Status: ✅ Token stored                            │
│  TTL: 3600 seconds (1 hour)                        │
└─────────────────────────────────────────────────────┘

Time: 0:30:00
┌─────────────────────────────────────────────────────┐
│  Redis: GET sso:token:abc123                       │
│  Status: ✅ Token exists                           │
│  TTL: 1800 seconds (30 min remaining)             │
└─────────────────────────────────────────────────────┘

Time: 0:59:59
┌─────────────────────────────────────────────────────┐
│  Redis: GET sso:token:abc123                       │
│  Status: ✅ Token exists                           │
│  TTL: 1 second (almost expired)                   │
└─────────────────────────────────────────────────────┘

Time: 1:00:00
┌─────────────────────────────────────────────────────┐
│  Redis: GET sso:token:abc123                       │
│  Status: ❌ Token not found                        │
│  Reason: Automatically deleted by Redis TTL ✅     │
└─────────────────────────────────────────────────────┘

User tries to use expired token:
→ Validation fails
→ Token rejected
→ User must log in again or use refresh token
```

### Refresh Token (Long-lived)

```
Time: 0:00:00
┌─────────────────────────────────────────────────────┐
│  Redis: SETEX sso:token:refresh_xyz 2592000 {...}  │
│  Status: ✅ Token stored                            │
│  TTL: 2592000 seconds (30 days)                    │
└─────────────────────────────────────────────────────┘

Time: 15 days later
┌─────────────────────────────────────────────────────┐
│  Redis: GET sso:token:refresh_xyz                  │
│  Status: ✅ Token exists                           │
│  TTL: 1296000 seconds (15 days remaining)         │
└─────────────────────────────────────────────────────┘

Time: 29 days later
┌─────────────────────────────────────────────────────┐
│  Redis: GET sso:token:refresh_xyz                  │
│  Status: ✅ Token exists                           │
│  TTL: 86400 seconds (1 day remaining)             │
└─────────────────────────────────────────────────────┘

Time: 30 days later
┌─────────────────────────────────────────────────────┐
│  Redis: GET sso:token:refresh_xyz                  │
│  Status: ❌ Token not found                        │
│  Reason: Automatically deleted by Redis TTL ✅     │
└─────────────────────────────────────────────────────┘

User tries to refresh:
→ Refresh token expired
→ User must re-authenticate
```

---

## 🔍 Token Validation Flow

```
┌─────────────────────────────────────────────────────────────┐
│                   Token Validation Request                 │
└─────────────────────────────────────────────────────────────┘
                              ↓
                    ┌─────────────────┐
                    │  Verify JWT     │
                    │  (signature)    │
                    └────────┬────────┘
                             ↓
                    ┌─────────────────┐
                    │  Check Redis    │
                    │  Does token     │
                    │  exist?         │
                    └────────┬────────┘
                             ↓
                ┌───────────────┴───────────────┐
                │                               │
           Yes  │                               │  No
                ▼                               ▼
        ┌──────────────┐               ┌──────────────┐
        │ Check Session│               │ Token        │
        │ is active?  │               │ Invalid/     │
        └──────┬───────┘               │ Expired ✅   │
               │                       └──────────────┘
        ┌──────┴──────┐
        │             │
   Yes  │             │  No
        ▼             ▼
┌─────────────┐  ┌─────────────┐
│ Token Valid │  │ Session     │
│   ✅        │  │ Expired     │
└─────────────┘  └─────────────┘
```

---

## 🗑️ Automatic Cleanup Process

### Without Redis (Problem)

```
Database Token Table:
┌─────────────────────────────────────────┐
│ token                | expiresAt        │
├─────────────────────────────────────────┤
| abc123...           | 2024-02-05 10:00  │
| def456...           | 2024-02-05 09:00  │ ← Expired!
| ghi789...           | 2024-02-05 11:00  │
│ ...                 | ...              │
└─────────────────────────────────────────┘

Problem:
❌ Expired tokens accumulate
❌ Need background cleanup job
❌ Database grows indefinitely
❌ Slower queries
❌ Higher costs

Solution Required:
→ Run cron job every hour
→ Query expired tokens
→ Delete from database
→ Additional complexity
```

### With Redis (Solution)

```
Redis Keys:
┌─────────────────────────────────────────┐
│ Key                  | TTL              │
├─────────────────────────────────────────┤
| sso:token:abc123     | 3600 seconds     │
| sso:token:def456     | EXPIRED ✅       │ ← Auto-deleted!
| sso:token:ghi789     | 7200 seconds     │
└─────────────────────────────────────────┘

Benefits:
✅ No expired tokens (auto-deleted)
✅ No cleanup job needed
✅ Memory stays consistent
✅ Fast queries
✅ Lower costs

Redis handles it automatically:
→ TTL reaches 0
→ Key is immediately deleted
→ No manual intervention
```

---

## 🚀 Token Refresh Flow

```
User: Request new access token
   ↓
Backend: Receive refresh token
   ↓
┌─────────────────────────────────────┐
│ Step 1: Verify JWT signature       │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│ Step 2: Check if token in Redis    │
└──────────────┬──────────────────────┘
               ↓
        ┌──────┴──────┐
        │             │
     Found        Not Found
        │             │
        ▼             ▼
┌─────────────┐  ┌─────────────┐
│ Check if    │  │ ❌ Invalid  │
│ refresh     │  │    token    │
│ token type  │  └─────────────┘
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────┐
│ Step 3: Check session is active   │
└──────────────┬──────────────────────┘
               ↓
        ┌──────┴──────┐
        │             │
     Active       Inactive
        │             │
        ▼             ▼
┌─────────────┐  ┌─────────────┐
│ Generate    │  │ ❌ Session  │
│ new tokens  │  │    expired  │
└──────┬──────┘  └─────────────┘
       │
       ▼
┌─────────────────────────────────────┐
│ Step 4: Store new tokens in Redis  │
│         (with new TTL)             │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│ Step 5: Delete old refresh token   │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│ Return new tokens to user          │
└─────────────────────────────────────┘
```

---

## 💡 Key Benefits of Redis TTL for Tokens

### 1. Automatic Cleanup
```typescript
// No need for this:
cron.schedule('0 * * * *', async () => {
  await db.token.deleteMany({
    where: { expiresAt: { lt: new Date() } }
  })
})

// Redis handles it automatically!
```

### 2. Immediate Expiration
```
Token TTL: 3600 seconds
At T+3600: Token instantly deleted (no waiting for cleanup job)
```

### 3. Memory Efficiency
```
Without Redis: Expired tokens occupy memory until cleanup
With Redis: Expired tokens removed immediately
```

### 4. Consistent Behavior
```
All tokens expire exactly at TTL
No variance based on cleanup job timing
```

---

## 📊 Real-World Example

### Scenario: User Logs In

```
14:00:00 - User logs in
         ↓
         Generate access token (abc123)
         Generate refresh token (xyz789)
         ↓
         Redis: SETEX sso:token:abc123 3600 {...}
         Redis: SETEX sso:token:xyz789 2592000 {...}
         ↓
         Return tokens to user
```

### Scenario: User Makes API Request

```
14:30:00 - User calls API with access token
         ↓
         Validate JWT signature ✅
         ↓
         Check Redis: GET sso:token:abc123
         ↓
         Token found ✅
         ↓
         Check session: Active ✅
         ↓
         Allow request
```

### Scenario: Access Token Expires

```
15:00:00 - Access token TTL reaches 0
         ↓
         Redis automatically deletes sso:token:abc123 ✅
         ↓
         User makes API request with same token
         ↓
         Validate JWT signature ✅
         ↓
         Check Redis: GET sso:token:abc123
         ↓
         Token not found ❌
         ↓
         Return 401 Unauthorized
         ↓
         Client uses refresh token to get new access token
```

### Scenario: Refresh Token Used

```
15:00:05 - Client calls refresh endpoint
         ↓
         Send refresh token (xyz789)
         ↓
         Validate JWT signature ✅
         ↓
         Check Redis: GET sso:token:xyz789
         ↓
         Token found ✅ (still valid, 30 days TTL)
         ↓
         Generate new access token (def456)
         Generate new refresh token (uvw901)
         ↓
         Redis: SETEX sso:token:def456 3600 {...}
         Redis: SETEX sso:token:uvw901 2592000 {...}
         Redis: DEL sso:token:xyz789
         ↓
         Return new tokens to user
```

---

## 🎯 Summary

### Yes! Redis is Used for Token Storage with Automatic Expiration

1. **Access Tokens**: Stored with 1 hour TTL
2. **Refresh Tokens**: Stored with 30 days TTL
3. **ID Tokens**: Stored with 1 hour TTL
4. **Auto-Cleanup**: Redis automatically removes expired tokens
5. **No Manual Intervention**: No cleanup jobs needed

### How It Works

```
SET key value TTL
    ↓
Redis stores with expiration time
    ↓
When TTL = 0, key is automatically deleted
    ↓
Token is no longer valid ✅
```

### Why This is Perfect

- ✅ Tokens have expiration → Redis TTL is ideal
- ✅ No expired token accumulation
- ✅ No manual cleanup jobs
- ✅ Fast validation
- ✅ Easy revocation
- ✅ Scalable to millions of tokens

---

**Last Updated**: 2026-02-05
**Status**: ✅ Redis TTL working perfectly for token storage
